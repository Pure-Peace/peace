syntax = "proto3";
package logs_rpc;

service LogsRpc {
  rpc ReloadLevel(ReloadLevelRequest) returns (CommonRpcResult);
  rpc DebugMode(DebugModeRequest) returns (CommonRpcResult);
}

enum Level {
  OFF = 0;
  ERROR = 1;
  WARN = 2;
  INFO = 3;
  DEBUG = 4;
  TRACE = 5;
}

message ReloadLevelRequest { Level level = 1; }

message DebugModeRequest { bool enabled = 1; }

message CommonRpcResult {
  bool status = 1;
  optional string msg = 2;
}
